// if else vs ternary
// closure in for loop?
// Deep copy vs Shallow Copy
// for await of loop
// Iterator and iteration protocol
// Symbols
// What is the difference between Object.freeze and Object.seal?
// Explain constructor functions.
// Explain prototypes
// Write polyfill for Array.flat
// Write debounce function

// Question//
console.log(a)
console.log(b)
var a = b = 5

// Question //
// Call vs bind

const foo = () => {
  console.log(this.name)
}
foo.call({ name: 'John' })

// Question//
// Make id readonly
const user = {
  id: 123,
  name: 'John Doe',
  age: 28,
  address: {
    city: 'Mumbai',
    state: 'Maharashtra',
  },
}

// Question //
// Debouncing vs Throttling

// Question //
// Explain mutation observer and intersection observer.
